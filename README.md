# `.github`

Organization-wide defaults for [DashinDesign](https://github.com/DashinDesign).

Files here apply to any repository that does not ship its own copy — a repository's
own file always wins. `profile/README.md` is the organization profile page.

A `LICENSE` cannot be defaulted; every repository needs its own.
